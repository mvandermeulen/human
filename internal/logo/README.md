# CLI Banner

The colorful `human` banner you see when starting the tool. It renders the name in a teal-to-pink gradient for a polished welcome.

- Shows the human name as ASCII art
- Paints it in a teal-gold-pink gradient
- Greets you when the wizard starts
- Adds a branded touch to the CLI
- Renders cleanly in your terminal
