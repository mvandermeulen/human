# Platform Detection

Helps `human` recognize the kind of system it is running on so it can adapt its behavior. Today it spots when you are inside Windows Subsystem for Linux.

- Detects when running under Windows Subsystem for Linux
- Lets the tool adapt to your system
- Avoids errors when detection is unavailable
- Keeps platform checks in one place
- Works quietly in the background
